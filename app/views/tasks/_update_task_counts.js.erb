var todo_counts = [<%= counts[:todo_h] %>,
                   <%= counts[:todo_m] %>, 
                   <%= counts[:todo_l] %>,
                   <%= counts[:doing] %>,
                   <%= counts[:waiting] %>,
                   <%= counts[:done] %>]
updateCounts( todo_counts.slice(0) );

