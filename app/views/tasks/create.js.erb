var id = <%= @task.id %>;
var li_html = '<%= (render "task", {:task => @task, :display => "none" }).gsub(/'/,"\\\\'").gsub(/\n/,"\\\n") %>';
var counts_json = <%= raw(@counts.to_json) %>

addTodoResponse(id, li_html, counts_json)

