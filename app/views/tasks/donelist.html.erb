<center>
<%= link_to "リストへ", tasks_path %>
</center>

<div class="donenote">
  <ul class="note">
  <% @tasks.each do |task| %>
    <li><%= task.updated_at.strftime("%Y年%m月%d日") %> <%= task.msg %></li>
  <% end %>
  </ul>
</div>


<%= will_paginate(@tasks) %>

